package com.minhaz.dsa;

public class Treaps {

    /* Operations */
    // Insert(X, Y) -> T(n) = O(lgN)
    // Search(X) -> T(n) = O(lgN)
    // Erase(X) -> T(n) = O(lgN)
    // Build(X1.........Xn) -> T(n) = O(N)
    // Union(T1, T2) -> T(n) = O(M log(N / M))
    // Intersect(T1, T2) -> T(n) = O(M log(N / M))
}
